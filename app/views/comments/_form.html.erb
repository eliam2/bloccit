<%= form_for [post, comment] do |f| %>

    <%= f.label :comment %>
    <%= f.text_area :body, rows: 2, class: 'form-control', placeholder: "Enter comment" %>

  <%= f.submit "Add comment", class: 'btn btn-success' %>
<% end %>